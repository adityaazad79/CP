#include <stdio.h>

int main()
{
    int a, s, n;
    scanf("%d", &a);
    for (int i = 0; i < a; i++)
    {
        scanf("%d%d", &n,&s);
        for (int j = 0; j < ; i++)
        {
            /* code */
        }
        
    }

    return 0;
}