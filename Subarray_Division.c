// /* https://www.hackerrank.com/challenges/the-birthday-bar/problem */
//Incomplete

#include <stdio.h>

int main()
{
    int n, m, d, temp, c;
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
    scanf("%d%d", &d, &m);
    for (int i = 0; i < n; i++)
    {
        
    }
    
    printf("%d", c);

    return 0;
}