// Incomplete

#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        int n, x, *a;
        scanf("%d%d", &n, &x);
        a = (int *)malooc(n * sizeof(int));
        
    }

    return 0;
}